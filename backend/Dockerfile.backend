FROM node:22-alpine

RUN apk add --no-cache \
    docker-cli \
    docker-cli-buildx \
    curl \
    python3 \
    py3-pip \
  && python3 -m pip install --break-system-packages ruff bandit

WORKDIR /app
